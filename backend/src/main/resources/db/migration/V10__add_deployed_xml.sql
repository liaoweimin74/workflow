-- ============================================================
-- V10: 新增 deployed_xml 字段，存储上次部署时的 BPMN XML
-- 用于部署前校验流程数据是否有变化，避免产生多余版本
-- ============================================================

alter table `wf_process_draft` add column `deployed_xml` LONGTEXT;
