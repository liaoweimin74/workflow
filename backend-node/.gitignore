# 依赖与构建产物
node_modules/
dist/
*.tsbuildinfo

# 机器本地配置（store/cache 路径与沙箱环境相关，不应提交）
.npmrc

# 真实环境变量（模板见 .env.example；绝不要把带密码/密钥的 .env 提交）
.env
.env.*
!.env.example

# pm2 运行期日志（ecosystem.config.cjs 指向这里）
logs/
